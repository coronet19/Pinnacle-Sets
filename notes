todo:

1. create a generic graph class that takes in a graph g of size n with some structuring.
    - this class should have functions for computing the number of labelings of g for a given
    pinnacle set p, and a generic function for iterating through the different pinnacle sets of g.
    - for a given pinnacle set P with size k, we need only check independent sets of size k
    - independent sets of size k can be generated from independent sets of size k-1


2. we can compute pinnacle sets of size k by looking at every independent set of size k
    - this lets us iterate through labelings faster

3. Given a graph G, and a pinnacle set P, if we remove an edge from G, does the number of labelings
    increase or decrease while maintaining P as a valid pinnacle set? collect examples of either
    - are there any examples where the number of labelings does not change?
    - stats on % that go up, % that go down, % that stay the same for graph of size n



binary string stuff:
1. google integer compositions
2. find a sequence that generates the number of permutations of a bin string length n with nonadjacent k ones
    - she thinks this is a binomial
3. google multinomials

If F(n,k) is the coefficient of xk in Fn(x), namely

    F n ( x ) = ∑ k = 0 n F ( n , k ) x k , {\displaystyle F_{n}(x)=\sum _{k=0}^{n}F(n,k)x^{k},\,}

then F(n,k) is the number of ways an n−1 by 1 rectangle can be tiled with 2 by 1 dominoes and 1 by 1 squares so that exactly k squares are used.[



a. clarify some background on exponential generating functions, specifically what i need to know for this proof (verify independently)
b. ask why multiplying the generating functions give us the coefficients of a function with n inputs
    - use generating functions to count the number of ways to add 3 positive numbers and get 57 (for example)
c. T(n, k) counts the ways to partition n−k elements into k or k−1 non-empty sets
    (depending on whether the edge gaps are treated as potential buckets)

what are combinatorial objects counted by the fibonacci numbers?
what are combinatorial objects that are counted by stirling numbers of the second kind, maybe find a bijection?
    - other formulas for stirling numbers of 2nd kind
    - expand the formulas to find any extra info about what theyre counting ie partitioning sets with constraints
ie fibonacci numbres count those 2x1 dominoes, so what do stirling numbers count?
if theres a (-1)^k, it has something to do with inclusion/exclusion arguments
ask ai for a citation on the formulas

i am not familiar with generating functions. why do we multiply the generating functions in G(x)=[E(x)]^2 × [I(x)]^(k−1)? and why are we raising them to some powers? what is x in this formula? and what does the formula tell us?
